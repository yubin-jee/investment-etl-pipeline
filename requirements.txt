# Meridian Capital - Script Dependencies
# Target runtime: Python 3.12+ (see pyproject.toml for the authoritative spec)
#
# These dependencies support the modernization objectives described in the
# README (proper ETL data handling, SQLAlchemy-based persistence, config via
# environment variables, and richer file format support).

pandas>=2.2,<3.0
sqlalchemy>=2.0,<3.0
python-dotenv>=1.0,<2.0
lxml>=5.0,<6.0
openpyxl>=3.1,<4.0
