# Meridian Capital - Legacy Script Dependencies
# NOTE: These scripts were written for Python 3.6 on Windows Server 2016
# They probably work on newer Python but nobody has tested
#
# ──────────────────────────────────────────────────────────────
# MODERNIZATION: See the option-specific requirements files for
# the new trade processing implementations:
#   modernized/requirements_option_a.txt  (Pandas + Pydantic)
#   modernized/requirements_option_b.txt  (Dagster)
#   modernized/requirements_option_c.txt  (Airflow)
# Full comparison: modernized/comparison.md
# ──────────────────────────────────────────────────────────────

# No external dependencies! Everything uses stdlib.
# Mike: "I don't trust pip, it broke our server once"
# 
# If we ever modernize, we should probably use:
# pandas>=1.5.0
# sqlalchemy>=2.0.0
# openpyxl>=3.0.0
# python-dotenv>=0.19.0
# lxml>=4.9.0
# schedule>=1.1.0
# boto3>=1.26.0  # if we move to AWS
