"""Option A: a standalone Pandas + Pydantic trade processor.

This package replaces the legacy ``legacy_scripts/process_trades.py`` with a
single, well-typed script that drives the shared modernization layer in
:mod:`modernized.common` (parsers, validation, settlement, config, models).
"""
